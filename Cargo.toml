[package]
name = "nova"
version = "0.1.0"
edition = "2021"


[profile.dev]
panic = "abort"
debug = true
opt-level = 0

[profile.release]
panic = "abort"

[dependencies]
libm = "0.2.15"
heap = {path = "heap"}
nova_error = {path = "nova_error"}

[workspace]

members = [ "nova_error",
    "heap"
]
